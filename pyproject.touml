[build-system]
requires = ["nanobind", "setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="dpp-dec-float",
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "nanobind",
]
